<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>도서 리뷰 작성</title>
    <style>
        .review-form {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
        }
        .form-group textarea {
            width: 100%;
            min-height: 100px;
            padding: 10px;
        }
        .rating-group {
            margin: 15px 0;
        }
        .button-group {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="review-form">
        <h2>도서 리뷰 작성</h2>
        <form action="/book/<%= isbn13 %>/review" method="POST">
            <div class="form-group">
                <label for="user_id">작성자 ID:</label>
                <input type="text" id="user_id" name="user_id" required>
            </div>
            
            <div class="form-group">
                <label for="rating">평점:</label>
                <select id="rating" name="rating" required>
                    <option value="">평점을 선택하세요</option>
                    <option value="5">⭐⭐⭐⭐⭐ (5점)</option>
                    <option value="4">⭐⭐⭐⭐ (4점)</option>
                    <option value="3">⭐⭐⭐ (3점)</option>
                    <option value="2">⭐⭐ (2점)</option>
                    <option value="1">⭐ (1점)</option>
                </select>
            </div>

            <div class="form-group">
                <label for="content">리뷰 내용:</label>
                <textarea id="content" name="content" placeholder="리뷰 내용을 입력해주세요" required></textarea>
            </div>

            <input type="hidden" name="book_id" value="<%= book_id %>">
            
            <div class="button-group">
                <button type="submit">리뷰 등록</button>
                <a href="/book/<%= isbn13 %>">
                    <button type="button">돌아가기</button>
                </a>
            </div>
        </form>
    </div>
</body>
</html>